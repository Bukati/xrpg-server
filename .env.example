# ========================================
# xRPG Backend Environment Variables
# ========================================

# Database
# Prisma connection string for PostgreSQL database
# For local development, use your local Postgres instance
# For production, use your hosted database (e.g., Neon, Supabase)
DATABASE_URL="postgresql://xxxx"

# Redis
# Redis connection string for caching, rate limiting, and BullMQ queues
# For local development: redis://localhost:6379
# For production: Use your hosted Redis instance
REDIS_URL="redis://localhost:6379"

# ========================================
# X API Credentials
# ========================================

# X API OAuth 1.0a - For app-level bot actions (future use)
# Get from: https://developer.x.com -> Your App -> Keys and tokens
# These credentials are for app-level actions (automated bot posts, monitoring)
# NOT used for user tweet posting (that uses OAuth 2.0 from frontend)
X_API_CONSUMER_KEY="your_consumer_key_here"
X_API_CONSUMER_SECRET="your_consumer_secret_here"
X_BEARER_TOKEN="your_bearer_token_here"
X_ACCESS_TOKEN="your_access_token_here"
X_ACCESS_TOKEN_SECRET="your_access_token_secret_here"

# X API OAuth 2.0 - User Authentication
# NOTE: OAuth 2.0 user authentication is handled by the frontend (Next.js)
# User access tokens are obtained during login and passed to backend via API proxy
# You DO NOT need to set OAuth 2.0 credentials here
# The frontend passes user tokens in X-Access-Token header

# ========================================
# AI Services
# ========================================

# Grok API
# Get from: https://x.ai
# Used for AI-powered content generation (scenarios, chapters, etc.)
GROK_API_KEY="your_grok_api_key_here"

# Parallel AI API
# Get from: https://parallel.ai
# Used for web search to find historical sources
PARALLEL_API_KEY="your_parallel_api_key_here"

# ========================================
# Server Configuration
# ========================================

# Port for Nest.js backend server
PORT=3001

# Node environment (development, production, test)
NODE_ENV="development"

# ========================================
# CORS Configuration
# ========================================

# Frontend origin for CORS configuration
# In development: http://localhost:3000
# In production: https://xrpg.gg
CORS_ORIGIN="http://localhost:3000"

# ========================================
# Optional Configuration
# ========================================

# Logging level (debug, info, warn, error)
# LOG_LEVEL="debug"

# Rate limiting
# RATE_LIMIT_TTL=60
# RATE_LIMIT_MAX=100